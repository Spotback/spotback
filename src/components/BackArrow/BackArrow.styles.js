import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 40,
    marginLeft: 40,
  },
  image: {
    width: 45,
    height: 45,
  },
});

export default styles;
