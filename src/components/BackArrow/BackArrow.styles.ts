import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingTop: 2.5,
    paddingLeft: 5,
  },
  image: {
    width: 45,
    height: 45,
  },
});

export default styles;
