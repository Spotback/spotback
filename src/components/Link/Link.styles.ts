import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
      container: {
        flexDirection: "row",
        flexWrap: "wrap"
      },
      extraText: {
          fontSize: 20,
          marginRight: 5
      },
      linkText: {
        fontSize: 20,
        color: "#70B81D",
      }
  });

  export default styles;